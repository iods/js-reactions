import React, { Component } from "react";

/*
 Import API data (if) render
 */

class App extends Component {

    // how is the setState() api used?
    state = {
        data: []
    };

    componentDidMount() {

        // NHL
        const url = "https://"
    }
}

export default App;